# Groq API — required for all LLM calls (text + vision)
# Get your key at https://console.groq.com
GROQ_API_KEY=gsk_your_key_here

# Groq model override — optional, defaults to llama-3.3-70b-versatile
GROQ_MODEL=llama-3.3-70b-versatile

# Figma Personal Access Token — required for Visual QA
# Figma → Settings → Security → Personal access tokens
FIGMA_API_TOKEN=your_figma_token_here

# MongoDB connection string — required for history and Visual QA jobs
# Local:  mongodb://localhost:27017
# Atlas:  mongodb+srv://<user>:<pass>@cluster.mongodb.net
MONGODB_URI=mongodb://localhost:27017

# CORS origins — restrict to your domain before production
CORS_ORIGINS=*

# Autonomous QA agent settings
OPENAI_API_KEY=
CLAUDE_API_KEY=
LLM_PROVIDER=groq
LLM_MODEL=llama-3.3-70b-versatile
LLM_TEMPERATURE=0.2
BROWSER_HEADLESS=true
BROWSER_TIMEOUT_MS=30000
MAX_RETRIES=3
TARGET_URL=
FIGMA_URL=
REQUIREMENTS_PATH=
OUTPUT_DIR=artifacts
VISUAL_DIFF_THRESHOLD=0.22
# JSON object map of per-page thresholds, e.g. {"/cart": 0.15, "/products": 0.2}
VISUAL_DIFF_PAGE_THRESHOLDS={}
STRICT_ACCESSIBILITY=false